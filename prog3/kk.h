#include <stdio.h>
#include <stdlib.h>
#include <stdbool.h>

#define ARR_LEN 100
#define MAX_ITER 25000

double cool_schedule(int i);
int rng(int min, int max);
double prob_rng(void);

long kk(long* array);
